# DEPRECATED — dependency source of truth is now pyproject.toml + uv.lock.
# Use `uv sync` to set up the environment. This file is kept as a fallback
# for environments without uv; run pip with the PyTorch index separately:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0
tqdm>=4.66.0
scipy>=1.11.0

# YOLO models (v8 and v10)
ultralytics>=8.2.0

# RT-DETR (via ultralytics or standalone)
# pip install -e git+https://github.com/lyuwenyu/RT-DETR.git if needed

# Experiment tracking
wandb>=0.16.0

# Data utilities
albumentations>=1.3.0
pycocotools>=2.0.7
supervision>=0.18.0

# Notebooks
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0

# Dev
black>=23.0.0
isort>=5.12.0
